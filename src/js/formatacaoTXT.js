function convertToUpperCase() {
    const input_cep = document.getElementById('CEP_LOCAL');
    input_cep.value = input_cep.value.toUpperCase();
    //
    const input_endereco = document.getElementById('ENDERECO');
    input_endereco.value = input_endereco.value.toUpperCase();
    //
    const input_bairro = document.getElementById('BAIRRO');
    input_bairro.value = input_bairro.value.toUpperCase();
    //
    const input_n = document.getElementById('N');
    input_n.value = input_n.value.toUpperCase();
    //
    const input_cidade = document.getElementById('CIDADE');
    input_cidade.value = input_cidade.value.toUpperCase();
    //
    const areatxt_descricao = document.getElementById('DESCREVA_OCORRIDO');
    areatxt_descricao.value = areatxt_descricao.value.toUpperCase();
}

function capsIndex() {
    const input_cpf_cnpj = document.getElementById('identifier');
    input_cpf_cnpj.value = input_cpf_cnpj.value.toUpperCase();
    //
    const input_senha = document.getElementById('senha');
    input_senha.value = input_senha.value.toUpperCase();
}

function capsRedefinirSenha() {
    const input_cpf = document.getElementById('cpf');
    input_cpf.value = input_cpf.value.toUpperCase();
    //
    const input_email = document.getElementById('email');
    input_email.value = input_email.value.toUpperCase();
    //
    const input_senha = document.getElementById('senha');
    input_senha.value = input_senha.value.toUpperCase();
}

function capsCadastroPf() {
    const input_nome = document.getElementById('nome');
    input_nome.value = input_nome.value.toUpperCase();
    //
    const input_email = document.getElementById('email');
    input_email.value = input_email.value.toUpperCase();
    //
    const input_cep = document.getElementById('cep');
    input_cep.value = input_cep.value.toUpperCase();
    //
    const input_bairro = document.getElementById('bairro');
    input_bairro.value = input_bairro.value.toUpperCase();
    //
    const input_n = document.getElementById('n');
    input_n.value = input_n.value.toUpperCase();
    //
    const input_senha = document.getElementById('senha');
    input_senha.value = input_senha.value.toUpperCase();
    //
    const input_cpf = document.getElementById('cpf');
    input_cpf.value = input_cpf.value.toUpperCase();
    //
    const input_ddd = document.getElementById('ddd');
    input_ddd.value = input_ddd.value.toUpperCase();
    //
    const input_telefone = document.getElementById('telefone');
    input_telefone.value = input_telefone.value.toUpperCase();
    //
    const input_endereco = document.getElementById('endereco');
    input_endereco.value = input_endereco.value.toUpperCase();
    //
    const input_cidade = document.getElementById('cidade');
    input_cidade.value = input_cidade.value.toUpperCase();
}

function formatInputCAEP() {
    const razao_social = document.getElementById('razao_social');
    razao_social.value = razao_social.value.toUpperCase();
    //
    const email = document.getElementById('email');
    email.value = email.value.toUpperCase();
    //
    const cep = document.getElementById('cep');
    cep.value = cep.value.toUpperCase();
    //
    const bairro = document.getElementById('bairro');
    bairro.value = bairro.value.toUpperCase();
    //
    const n = document.getElementById('n');
    n.value = n.value.toUpperCase();
    //
    const cnpj = document.getElementById('cnpj');
    cnpj.value = cnpj.value.toUpperCase();
    //
    const ddd = document.getElementById('ddd');
    ddd.value = ddd.value.toUpperCase();
    //
    const telefone = document.getElementById('telefone');
    telefone.value = telefone.value.toUpperCase();
    //
    const endereco = document.getElementById('endereco');
    endereco.value = endereco.value.toUpperCase();
    //
    const cidade = document.getElementById('cidade');
    cidade.value = cidade.value.toUpperCase();
}